{{- range .Values.services }}
kind: Service
apiVersion: v1
metadata:
  name: {{ template "conan-ci.fullname" $ }}-{{ .name }}
spec:
  selector:
    app: {{ template "conan-ci.fullname" $ }}-{{ .name }}
  ports:
    - port: 8080
---
{{- end }}
